"use client";import { useState } from "react";
export function VerifyForm({type,id}:{type:"attendance"|"stock_check";id:string}){const [message,setMessage]=useState("");return <form className="card" onSubmit={async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const r=await fetch("/api/admin/verify",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({recordType:type,recordId:id,status:fd.get("status"),note:fd.get("note")})});const j=await r.json();setMessage(r.ok?"Verifikasi disimpan.":j.error);if(r.ok)setTimeout(()=>location.reload(),700)}}><h2>Verifikasi admin</h2><div className="field"><label>Status</label><select name="status"><option value="verified">Valid</option><option value="needs_review">Perlu diperiksa</option><option value="rejected">Ditolak</option></select></div><div className="field"><label>Catatan</label><textarea name="note" maxLength={1000}/></div><button className="btn btn-primary" style={{marginTop:10}}>Simpan verifikasi</button>{message&&<p>{message}</p>}</form>}
