import { AttendanceForm } from "@/components/attendance-form";import { requireRole } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";
export default async function Attendance(){const p=await requireRole("employee");const sb=await createClient();const today=new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Jakarta"}).format(new Date());const {data:a}=await sb.from("attendances").select("check_in_at,check_out_at,status").eq("user_id",p.id).eq("attendance_date",today).maybeSingle();return <><h1>Presensi</h1>{a?.check_out_at?<div className="card"><h2>Presensi hari ini selesai</h2><p>Masuk dan pulang sudah tercatat. Catatan tidak dapat diubah.</p></div>:<AttendanceForm canCheckOut={Boolean(a?.check_in_at)}/>}</>}
