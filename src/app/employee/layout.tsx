import {requireRole} from "@/lib/auth";import {PwaRegister} from "@/components/pwa-register";import {EmployeeBottomNav} from "@/components/employee-bottom-nav";import {getEmployeeTodayState} from "@/lib/operations";
export const dynamic="force-dynamic";
export default async function EmployeeLayout({children}:{children:React.ReactNode}){const profile=await requireRole("employee");const {attendance,stock}=await getEmployeeTodayState(profile.id);return <><PwaRegister/><header className="mobile-header"><div className="container"><div className="brand-mark">🐓</div><div><strong>Rocky Rooster</strong><small>Booth Control</small></div></div></header><main className="employee-main container">{children}</main><EmployeeBottomNav stockPending={!stock} attendancePending={Boolean(attendance?.check_in_at&&!attendance.check_out_at)}/></>}
