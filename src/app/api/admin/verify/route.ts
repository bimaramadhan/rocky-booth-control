import { NextResponse } from "next/server";import { requireRole } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";import { verificationSchema } from "@/lib/validation";
export async function POST(req:Request){await requireRole("admin");try{const i=verificationSchema.parse(await req.json());const {error}=await (await createClient()).rpc("verify_record",{p_type:i.recordType,p_id:i.recordId,p_status:i.status,p_note:i.note});if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Gagal"},{status:400})}}
